

export default class LeafVariableData{
    identifier?: string
    value?: number;
    constructor(identifier: string, value: number) {
        this.identifier = identifier;
        this.value = value;
    }
}